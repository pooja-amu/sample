#module.exports = (robot) ->
 # xls = require 'xlsx'
  #workbook = xls.readFile('C:\\Users\\pbuddaraju\\myhubot\\files\\day1.xlsx')
  #sheet = table.Sheets[table.sheetNames[0]]
  #sheet_name_list = workbook.SheetNames;
  #var = xls.utils.sheet_to_json(workbook.Sheets[sheet_name_list[0]])
  #if i(date) is 1-Jan
   # out = var(i)
  #robot.respond /show excel/i,(res) ->
   # res.send "#{var}"
